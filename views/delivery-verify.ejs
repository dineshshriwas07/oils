<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Delivery Verification</title>

    <!-- favicon  -->
    <link rel="icon" type="image/png" href="/img/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/img/favicon.svg" />
    <link rel="shortcut icon" href="/img/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/img/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="MyWebSite" />
    <link rel="manifest" href="/img/site.webmanifest" />

    <!-- css file link  -->
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link rel="stylesheet" type="text/css" href="/css/media.css">

    <!-- bootstrap cdn link  -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- remix icon cdn  -->
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
    rel="stylesheet"/>

    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    <!-- font family use  -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
  <style>
    body {
      background: linear-gradient(135deg, #74ebd5 0%, #9face6 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .verify-card {
    background: #fff;
    border-radius: 20px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    padding: 30px;
    width: 100%;
    max-width: 480px;
    animation: fadeIn 0.6s ease-in-out;
    }
    .verify-card h2 {
        font-weight: 700;
        font-size: 1.8rem;
        text-align: center;
        margin-bottom: 20px;
        color: #333;
    }
    .form-label {
        font-weight: 500;
        color: #444;
    }
    .form-control {
        border-radius: 12px;
        padding: 12px;
        border: 1px solid #ddd;
    }
    .btn-custom {
        background: #399c87;
        border: none;
        border-radius: 12px;
        padding: 12px;
        font-weight: 600;
        letter-spacing: 0.5px;
        transition: 0.3s;
    }
    .btn-custom:hover {
        background: #2e7a6a;
    }
    .btn-secondary-custom {
        background: #6c757d;
        border: none;
        border-radius: 12px;
        padding: 10px 16px;
        font-weight: 500;
        transition: 0.3s;
    }
    .btn-secondary-custom:hover {
        background: #565e64;
    }
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>

<div class="verify-card">
  <h2>Delivery Verification</h2>

  <% if (message) { %>
    <div class="alert alert-<%= color === 'green' ? 'success' : 'danger' %> text-center">
      <%= message %>
    </div>
  <% } %>

  <form method="POST" action="/delivery-verify">
      <div class="mb-3">
        <label class="form-label">Customer Mobile (or Email)</label>
        <input name="identifier" type="text" class="form-control" placeholder="e.g. 9876543210 OR user@email.com" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Delivery Code (6 digits)</label>
        <input name="code" type="text" class="form-control" maxlength="6" minlength="6" pattern="[0-9]{6}" placeholder="Enter 6-digit code" required>
      </div>
      <button class="btn btn-custom w-100 mb-3">Verify & Complete</button>
  </form>

  <!-- Reload and Back Buttons -->
  <div class="d-flex justify-content-between">
    <button class="btn btn-secondary-custom w-50 me-2" onclick="window.location.reload();">🔄 Reload</button>
    <button class="btn btn-secondary-custom w-50 ms-2" onclick="window.history.back();">⬅ Back</button>
  </div>
</div>

</body>
</html>
